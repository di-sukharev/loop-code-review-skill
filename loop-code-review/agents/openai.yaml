interface:
  display_name: "Loop Code Review"
  short_description: "Iterative independent review-and-fix loop"
  default_prompt: "Use $loop-code-review to run the iterative independent review workflow on only this task's active changes."
